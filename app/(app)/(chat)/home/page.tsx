// /home — chat-surface entry point. The visible surface is owned by
// the (chat) route group's layout; this page is a stub so the route
// resolves. See docs/Clevroy_Chat_Surface.md §13.

export default function HomePage() {
  return null;
}
