/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true
  // typedRoutes intentionally off during multi-agent scaffolding —
  // re-enable once /create, /projects, /settings/* etc. exist as files.
};

export default nextConfig;
